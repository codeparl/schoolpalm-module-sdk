import axios from 'axios';

const api = axios.create({
    baseURL: `${(new URL(document.referrer).origin)}/api/{{ context }}/{{ module }}`,
    headers: {
        'X-Requested-With': 'XMLHttpRequest',
        'Accept': 'application/json'
    }
});

export default api;
